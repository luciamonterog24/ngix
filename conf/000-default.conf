server {
    listen 80 default_server;
    server_name _;

    # Aquí va la ruta de la carpeta por defecto
    root /var/www/html;
    index index.html index.nginx-debian.html;

    location / {
        try_files $uri $uri/ =404;
    }
}
